import 'package:flutter/material.dart';

class AtenderWeb extends StatelessWidget {
  const AtenderWeb({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple[200],
    );
  }
}